title: scoring
command: python scoring.py
description: "Scoring step: reads participant's /output and compares to reference ECG"

# Parameter Description:
# /input: The directory containing the results generated by the user's algorithm (corresponding to the /output of ingestion)
# /truth: The reference data directory (if reference_data is configured)
# /output: The output directory for scoring results (generates scores.json)